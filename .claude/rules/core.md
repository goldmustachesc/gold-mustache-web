---
paths:
  - "**/*.ts"
  - "**/*.tsx"
---

# Core

- TypeScript obrigatório. Imports com `@/`.
- Proibido `any`. Usar tipos explícitos, `unknown` com narrowing, generics.
- TDD: testes antes da implementação para código novo.
- Biome: seguir `biome.json`.
- Conventional Commits.
